# 프로필: local(개발자 노트북), test(Gradle 테스트), dev(개발 서버), prod(운영). 사용법은 docs/profiles.md.
# 프로필을 지정하지 않으면 local로 뜬다. 서버는 SPRING_PROFILES_ACTIVE로 명시한다.
spring:
  config:
    import: optional:file:.env[.properties]
  profiles:
    default: local

  # schema.sql이 DROP TABLE로 시작하므로 공통 기본값은 never다. local과 test만 always로 켠다.
  sql:
    init:
      mode: never

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

oauth:
  providers:
    kakao:
      client-id: ${KAKAO_CLIENT_ID:}
      client-secret: ${KAKAO_CLIENT_SECRET:}
      redirect-uri: ${KAKAO_REDIRECT_URI:}
    google:
      client-id: ${GOOGLE_CLIENT_ID:}
      client-secret: ${GOOGLE_CLIENT_SECRET:}
      redirect-uri: ${GOOGLE_REDIRECT_URI:}
    apple:
      client-id: ${APPLE_CLIENT_ID:}
      team-id: ${APPLE_TEAM_ID:}
      key-id: ${APPLE_KEY_ID:}
      private-key: ${APPLE_PRIVATE_KEY:}

jwt:
  secret: ${JWT_SECRET}
  issuer: yeogidam
  access-token-ttl: 30m
  refresh-token-ttl: 30d

# 앱 업데이트 정책. 최소 버전 아래는 강제, 최신 버전 아래는 권고. 바꿀 때 재배포한다. 운영 값은 application-prod.yml에서 덮는다.
app-update:
  ios:
    minimum-supported-version: 1.0.0
    latest-version: 1.1.0
    store-url: https://apps.apple.com/kr/app/id0000000000
  android:
    minimum-supported-version: 1.0.0
    latest-version: 1.1.0
    store-url: https://play.google.com/store/apps/details?id=com.yeogidam.app
